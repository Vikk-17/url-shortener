sqlx command
sqlx command
sqlx migrate add -r init
sqlx migrate run
sqlx migrate revert
#
# Docker Command
docker tag local-image:tagname new-repo:tagname
docker push new-repo:tagname
# or build the image with the same name of the repo
# then use the push command with the name of the repo

